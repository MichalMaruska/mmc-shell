#! /usr/bin/zsh -f

if [ -z "$(git tag -l)" ]
then
    echo "no tags at all"
else
    last_tag=$(git describe $(git rev-list --tags --max-count=1))
#    if [[ -z $last_tag ]]
#    then
    git log --oneline --max-count=10 ${last_tag}..
#    fi
fi
